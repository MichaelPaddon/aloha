[INCLUDES]
before = common.conf

[Definition]
# Match HTTP 401 authentication failures emitted by aloha at WARN level.
# The peer field is IP:port; <HOST> extracts just the address.
failregex = WARN aloha::listener: auth failed peer=<HOST>:\d+\b

ignoreregex =
